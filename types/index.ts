// This is a new file, so we start from scratch.
// Since the instructions say to remove any tutorial-related types,
// we will create a basic types file without any tutorial types.

export type User = {
  id: string
  name: string
  email: string
  // other user properties
}

export type Product = {
  id: string
  name: string
  price: number
  description: string
  // other product properties
}

// Add more types as needed for your application.
